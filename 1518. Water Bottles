var numWaterBottles = function(numBottles, numExchange) {
    let restEmpty = 0;
    let prevRestEmpty = 0;
    let curBotle = numBottles;
    if (numExchange > numBottles){
        let result = numBottles;
        return result;
    }

    if (numExchange === numBottles){
        let result = numBottles + 1
        return result
    }

    for (let i = curBotle; i > numExchange; i = i - restEmpty){

    }

    // while (curBotle >= numExchange) {
    //     restEmpty = curBotle % numExchange;
    //     console.log(restEmpty)        
    // }


    
};

console.log(numWaterBottles(15, 4))
