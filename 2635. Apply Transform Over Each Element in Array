/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    let resArr = [];
    for (let i = 0; i < arr.length; i += 1){
        let res = fn(arr[i], i);
        resArr.push(res)
    }
    return resArr;    
};
